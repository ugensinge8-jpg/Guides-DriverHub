# DrukConnect — deploy

Upload every file here to a GitHub repo (all at the top level), then connect the repo to
Cloudflare Pages with build command `npm run build` and output directory `dist`.
Cloudflare installs, builds, and hosts it over HTTPS. Editing files on GitHub redeploys it.
